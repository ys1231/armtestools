
#pragma GCC diagnostic error "-std=c++11"
#include"armtools.h"

int main() {

    armtools armtools_;
	return 0;

}
      

/*
 * #include <string.h>
#include <stdarg.h>
#include<time.h>
#include<fstream>
#include <iostream>


using namespace std;

void mysprintf(const char*fmt,...){

        char buffer[512];   // 分配512字节的字符串缓存
        memset(buffer, 0, 512);
        va_list aptr;
        va_start(aptr, fmt);
        vsnprintf(buffer,sizeof(buffer) ,fmt, aptr);
        va_end(aptr);
        puts(buffer);
}

int main(){

        mysprintf("%d%shahdushfciaDHUASFHUIA",10,"10HAHA");


        return 0;

}

 */